Description: Pass "nowarn" to javac ant task to disable printing of warning
 during compilation.
--- a/src/build.xml
+++ b/src/build.xml
@@ -57,6 +57,7 @@
            debug="${debug}"
        target="${target-jvm}"
        source="${source-level}"
+       nowarn="true"
        >
     </javac>
     <copy todir="${classes}">
@@ -76,6 +77,7 @@
            debug="${debug}"
        target="${target-jvm}"
        source="${source-level}"
+       nowarn="true"
        >
     </javac>
   </target>
